server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/victorferx/trabajoFinal.git
          searchPaths: config-server
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

  security:
    user:
      name: sa
      password: toor